class State {
  String name;
  String stateId;
  String localId;

  State({this.name, this.stateId, this.localId});

  // State.fromJson(Map<String, dynamic> json):
  //   json['name'] = name, json['id'] = id;
}
